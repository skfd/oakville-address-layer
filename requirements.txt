# The build pipeline is the address-layer engine (sibling repo). Install it
# editable so local engine edits take effect immediately:
-e ../address-layer
# If the engine is on GitHub instead of a sibling checkout, replace the line
# above with, e.g.:
#   address-layer @ git+https://github.com/skfd/address-layer.git
