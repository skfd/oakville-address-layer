# The build pipeline is the address-layerist engine (sibling repo). Install it
# editable so local engine edits take effect immediately:
-e ../address-layerist
# If the engine is on GitHub instead of a sibling checkout, replace the line
# above with, e.g.:
#   address-layerist @ git+https://github.com/skfd/address-layerist.git
